# 机器人项目总进度与完整实施流程

更新时间：2026-08-28

本文是当前机器人项目的总控文档。它把底板、STM32、KICKPI、K230、视觉模型、Web
页面和数据库放在同一条实施链路中。执行时必须按照“先单模块、再分段联调、最后整机”的
顺序进行，不要一开始就让电机、视觉和自动控制同时运行。

## 1. 项目最终目标

```text
K230 摄像头和屏幕
  |-- 显示实时画面
  |-- 运行 Cron 锈病识别模型
  |-- 屏幕触摸控制前进、后退、左转、右转、停止
  |-- 通过以太网 HTTP 上传识别结果和控制命令
        |
        v
KICKPI H618，192.168.1.119
  |-- ROS2 Humble
  |-- robot_gateway：HTTP API、Web 页面、SQLite 数据库
  |-- robot_base_bridge：ROS2 与 STM32 串口桥接
        |
        | UART5 /dev/ttyAS5，115200，3.3V TTL
        v
STM32F103C8T6 最小系统板
  |-- 接收速度和状态命令
  |-- TB6612 控制两个编码电机
  |-- 读取编码器、DHT30、MPU6050、DS18B20、INA219
  |-- 故障、欠压、通信超时和急停保护
```

K230 不直接连接 STM32，也不直接生成电机 PWM。KICKPI 是上层控制入口，STM32 保留
最后一级实时保护。

## 2. 当前总进度

这里的“进度”按交付链路估算，不等于已经通过整机安全验收。当前软件准备度约为 **75%**，
整机可用度约为 **45%**。主要差距在于：K230 尚未实机运行、STM32 正式二进制通信尚未
与 KICKPI 联调、INA219 真实电池接线和整车电机测试尚未完成。

| 模块 | 状态 | 当前结论 |
| --- | --- | --- |
| 需求和总体架构 | 已完成 | 已确定 K230 -> KICKPI -> ROS2 -> UART -> STM32 的链路 |
| 底板原理图 | 基本完成 | 已按 STM32 最小系统板、TB6612 模块、传感器模块和继电器规划 |
| 底板 PCB | 已有设计 | 仍需制造后检查焊接、供电压降、间距和电机大电流路径 |
| STM32 CubeMX 配置 | 已完成 | USART1、I2C2、TIM1 PWM、TIM2/TIM4 编码器和 FreeRTOS 已配置 |
| STM32 调试固件 | 已完成并编译 | UART1 文本调试和传感器日志已经保留；继电器测试已关闭 |
| STM32 正式协议固件 | 待切换验证 | 需要把 `ROBOT_UART1_DEBUG_ONLY` 改为 `0` 后重新编译烧录 |
| DHT30 | 已验证 | 地址为 `0x38`，可以读取温度和湿度 |
| MPU6050 | 已验证 | 当前挂在 PB10/PB11 的 I2C2，总线地址按实际模块配置 |
| DS18B20 | 已验证 | 已有有效温度日志 |
| INA219 | 未完成 | 之前未上电；当前只用于电池电压检测，必须先完成真实电池接线 |
| 电机和编码器 | 部分完成 | 引脚和代码框架已经有，仍需整机方向、极性和闭环验证 |
| 继电器 | 已验证但当前关闭 | 用户确认继电器正常；当前代码和 PB1 均关闭，不参与运行 |
| KICKPI ROS2 底盘桥 | 已部署并构建 | `robot_base_bridge` 已在 KICKPI 生成并可发现 |
| KICKPI Web/API/SQLite | 已实现并烟雾测试 | `robot_gateway` 的 HTTP、视觉事件和数据库写入已测试 |
| KICKPI systemd 服务 | 已安装但停用 | 两个服务故意保持 disabled/inactive，避免误动车 |
| K230 资料解析 | 已完成 | 已确认 CanMV、LAN、触摸屏、PipeLine 和 DetectionApp 用法 |
| Cron 模型解析 | 已完成 | 已确认是 `AnchorBaseDet`，唯一类别是 `rust` |
| Cron 模型复制 | 已完成 | 模型和配置已经复制到 K230 应用目录，SHA256 一致 |
| K230 屏幕控制程序 | 已编写 | 已包含摄像头、触摸按钮、HTTP 控制和状态叠加 |
| K230 视觉程序 | 已接入代码 | 已按真实 `deploy_config.json` 初始化 AnchorBaseDet |
| K230 实机推理 | 未完成 | Windows 无 K230 NPU/CanMV 运行时，必须放入 K230 实机执行 |
| 浏览器显示真实传感器 | 待联调 | Web 页面已经有接口，等待 STM32 正式数据进入 ROS2 |
| 整机验收 | 未完成 | 需要完成断网停车、故障停车、急停、低压和长时间运行测试 |

### 2.1 Cron 模型的确定结果

模型目录：

```text
D:\cxdownload\end\Cron
```

当前模型文件：

```text
best_AnchorBaseDet_can2_5_s_20260827131049.kmodel
```

部署配置：

```text
model_type: AnchorBaseDet
categories: ["rust"]
num_classes: 1
img_size: [320, 320]
confidence_threshold: 0.4
nms_threshold: 0.5
nms_option: false
```

Anchor 配置为：

```text
[43, 40, 63, 88, 104, 67]
[76, 153, 111, 128, 169, 95]
[96, 236, 144, 226, 206, 181]
```

模型 SHA256：

```text
E6E49E7015F399F6AB3259E34E202439F61CB7C6A5EA80D3DDFD658318625228
```

`det_results` 中已经有带 `rust` 检测框的示例图片，说明训练模型的类别和部署结果格式
已经可以识别。当前没有在 Windows 上直接运行 `.kmodel`，因为该模型依赖 K230 的 NPU、
CanMV 固件和 `libs.PlatTasks.DetectionApp` 运行环境。

## 3. 已完成文件和位置

### 3.1 STM32

```text
D:\cxdownload\end\code\code.ioc
D:\cxdownload\end\code\Core\Inc\robot_config.h
D:\cxdownload\end\code\MDK-ARM\code\code.hex
```

当前调试固件曾经成功编译，Keil 结果为 0 error、0 warning。当前调试阶段使用 UART1
输出中文/英文状态文本；正式接入 KICKPI 前必须切换为二进制协议模式。

### 3.2 KICKPI

```text
D:\cxdownload\end\code\kickpi_ros2_ws\src\robot_base_bridge
D:\cxdownload\end\code\kickpi_ros2_ws\src\robot_gateway
D:\cxdownload\end\code\kickpi_ros2_ws\robot-base-bridge.service
D:\cxdownload\end\code\kickpi_ros2_ws\robot-gateway.service
```

KICKPI 参数：

```text
IP：192.168.1.119
系统：Ubuntu 22.04.5
架构：aarch64
ROS：ROS 2 Humble
串口：/dev/ttyAS5
串口参数：115200 8N1，无硬件流控
```

### 3.3 K230

```text
D:\cxdownload\end\code\k230_canmv_apps\robot_touch_drive.py
D:\cxdownload\end\code\k230_canmv_apps\det_image.py
D:\cxdownload\end\code\k230_canmv_apps\test.jpg
D:\cxdownload\end\code\k230_canmv_apps\libs\
D:\cxdownload\end\code\k230_canmv_apps\mp_deployment_source\deploy_config.json
D:\cxdownload\end\code\k230_canmv_apps\mp_deployment_source\best_AnchorBaseDet_can2_5_s_20260827131049.kmodel
```

复制时，把 `k230_canmv_apps` 目录中的内容复制到 K230 SD 卡根目录 `/sdcard/`，最终结构
应为：

```text
/sdcard/robot_touch_drive.py
/sdcard/det_image.py
/sdcard/test.jpg
/sdcard/libs/
/sdcard/mp_deployment_source/deploy_config.json
/sdcard/mp_deployment_source/best_AnchorBaseDet_can2_5_s_20260827131049.kmodel
```

## 4. 硬件和接线验收

### 4.1 KICKPI 到 STM32

```text
KICKPI 8脚  UART5_TX -> STM32 PA10 / USART1_RX
KICKPI 10脚 UART5_RX <- STM32 PA9  / USART1_TX
KICKPI 6脚  GND      -> STM32 GND
```

必须交叉 TX/RX，必须共地，信号只使用 3.3V TTL。不能把 5V 接到 UART 信号脚。

### 4.2 STM32 当前关键资源

| 功能 | STM32 资源 |
| --- | --- |
| KICKPI 通信 | USART1，PA9/PA10 |
| DHT30、MPU6050、INA219 | I2C2，PB10/PB11 |
| 左电机编码器 | TIM2 Encoder，PA0/PA1 |
| 右电机编码器 | TIM4 Encoder，PB6/PB7 |
| PWM | TIM1_CH1 PA8、TIM1_CH4 PA11 |
| TB6612 方向 | PB12/PB13、PB14/PB15 |
| TB6612 STBY | PA6 |
| DS18B20 | PA4 |
| 继电器 | PB1 |
| 蜂鸣器 | PB0，但当前代码关闭 |
| 调试串口 | USART1；调试阶段文本，正式阶段二进制 |

### 4.3 INA219 电池电压检测

当前 INA219 的用途是检测电池电压，不是承担整车大电流测量。应按下面连接：

```text
电池正极 / BAT+ -> INA219 VIN+
INA219 VIN-      -> BAT_SENSE+
BAT_SENSE+       -> 底板电池正输入 / 降压模块正输入
电池负极 / BAT- -> 底板 GND
INA219 GND      -> 底板 GND
INA219 VCC      -> 3.3V
INA219 SDA/SCL  -> PB11/PB10
```

如果只是电压检测，不要把 INA219 的 `VIN-` 悬空，也不要把电池负极接到 `VIN-`。
电池负极属于 GND，电池正极电流路径经过 `VIN+ -> VIN-`。模块上的分流电阻允许的
电流要符合实际电机启动和堵转电流；若模块只有 3A 级分流器，它不应串在电机大电流主
回路中，建议只放在电池监测支路，或者更换适合峰值电流的检测方案。

INA219 未上电或未正确接线时，STM32 可能报告欠压故障 `FAULT=0x0002`，此时电机不应
作为通信故障判断依据。

## 5. STM32 固件实施顺序

### 阶段 A：继续使用串口文本调试

1. 保持 `ROBOT_UART1_DEBUG_ONLY=1`，当前先用 UART1 文本观察数据。
2. 关闭蜂鸣器，保持 `ROBOT_BUZZER_ENABLE=0`。
3. 保持 `ROBOT_RELAY_ENABLE=0` 和 `ROBOT_RELAY_TEST_ENABLE=0`，当前不使用继电器。
4. 给 STM32 5V 供电，确认最小系统板板载稳压芯片输出 3.3V 正常。
5. 用串口助手观察 UART1：DHT30、DS18B20、MPU6050、INA219 和故障状态。
6. INA219 接线完成后，确认读到的电池电压接近万用表实测值。
7. 确认电机断开时不会因为错误的传感器读数持续产生异常故障。

### 阶段 B：验证电机和编码器

1. 小车架空，电机轮子离地。
2. 确认 `STBY`、PWM、方向线电平正确。
3. 单独让左轮低速运行，观察左编码器计数方向。
4. 单独让右轮低速运行，观察右编码器计数方向。
5. 如果前进时一侧计数方向相反，在软件中修正方向符号，不要先改乱 PCB 走线。
6. 检查停车时两侧 PWM 都回到 0，编码器计数仍可读取。

### 阶段 C：切换正式二进制协议

在串口文本调试和电机架空测试都通过后：

1. 在 `D:\cxdownload\end\code\Core\Inc\robot_config.h` 设置：

```c
#define ROBOT_UART1_DEBUG_ONLY 0
#define ROBOT_RELAY_TEST_ENABLE 0
#define ROBOT_BUZZER_ENABLE 0
```

2. 重新编译 Keil 工程。
3. 确认生成新的 `code.hex`。
4. 烧录 STM32。
5. 先不启动 KICKPI 桥，检查上电后无持续误动作。
6. 再用 KICKPI 发送 `enable`、零速度和 `clear_fault` 测试帧。

## 6. KICKPI ROS2 实施顺序

### 6.1 编译

SSH 登录 KICKPI 后执行：

```bash
cd /root/kickpi_ros2_ws
source /opt/ros/humble/setup.bash
colcon build --symlink-install
source install/setup.bash
```

### 6.2 先启动底盘桥

```bash
source /opt/ros/humble/setup.bash
source /root/kickpi_ros2_ws/install/setup.bash
ros2 launch robot_base_bridge base_bridge.launch.py port:=/dev/ttyAS5 baud:=115200
```

另开终端查看：

```bash
ros2 topic list
ros2 topic echo /base/faults
ros2 topic echo /battery_state
ros2 topic echo /env/temperature
ros2 topic echo /env/humidity
ros2 topic echo /odom
```

如果 `/base/faults` 持续显示欠压，先处理 INA219 和电池接线，不要靠屏蔽故障继续开车。

### 6.3 再启动 Web/API 网关

```bash
source /opt/ros/humble/setup.bash
source /root/kickpi_ros2_ws/install/setup.bash
mkdir -p /root/robot_data
ros2 launch robot_gateway gateway.launch.py \
  host:=0.0.0.0 \
  port:=8080 \
  db_path:=/root/robot_data/robot.db \
  token:=请替换为长随机字符串 \
  control_timeout_s:=0.3
```

健康检查：

```bash
curl http://127.0.0.1:8080/api/v1/health
curl http://127.0.0.1:8080/api/v1/latest
```

浏览器地址：

```text
http://192.168.1.119:8080
```

### 6.4 控制命令接口

控制请求必须带 `X-Device-Token`。测试时先只发送停车：

```bash
curl -H 'X-Device-Token: 你的令牌' \
  -H 'Content-Type: application/json' \
  -d '{"source":"curl","button":"stop","linear_x_m_s":0,"angular_z_rad_s":0}' \
  http://127.0.0.1:8080/api/v1/control/cmd_vel
```

可用状态命令：

```text
enable
stop
clear_fault
estop
```

网关限制：线速度最大约 `0.20 m/s`，角速度最大约 `1.20 rad/s`。控制命令超过约
`300 ms` 没有新的心跳时，网关自动发布零速度。

## 7. K230 模型和屏幕实施顺序

### 7.1 复制文件

将下面目录的内容复制到 K230 SD 卡 `/sdcard/`：

```text
D:\cxdownload\end\code\k230_canmv_apps\
```

最终结构必须是：

```text
/sdcard/robot_touch_drive.py
/sdcard/libs/
/sdcard/mp_deployment_source/deploy_config.json
/sdcard/mp_deployment_source/best_AnchorBaseDet_can2_5_s_20260827131049.kmodel
```

### 7.2 第一次只测试屏幕、摄像头和网络

先在 `robot_touch_drive.py` 中设置：

```python
ENABLE_VISION = False
```

然后确认：

1. 屏幕可以显示摄像头画面。
2. 触摸坐标方向正确。
3. 前进、后退、左转、右转、停车按钮坐标正确。
4. 按住方向按钮才运动，松手立即停车。
5. K230 能访问 `192.168.1.119:8080`。
6. KICKPI 返回的电压、温湿度和故障码能显示在屏幕上。

### 7.3 第二次打开真实模型

确认上一阶段通过后设置：

```python
ENABLE_VISION = True
```

程序会自动读取：

```text
/sdcard/mp_deployment_source/deploy_config.json
```

并按 `AnchorBaseDet` 初始化 `DetectionApp`。识别结果原始格式为：

```python
{
    "boxes": [[x1, y1, x2, y2]],
    "scores": [score],
    "idx": [class_id]
}
```

程序会转换成网关格式：

```json
{
  "label": "rust",
  "class_id": 0,
  "score": 0.93,
  "x": 120,
  "y": 80,
  "w": 190,
  "h": 160
}
```

当前视觉结果默认每 `500 ms` 上传一次；控制心跳目标为每 `100 ms` 一次。模型推理或
网络异常时，程序应该保留停车能力，不能因为识别异常继续保持电机运行。

## 8. Web 和数据库流程

### 8.1 数据来源

`robot_base_bridge` 从 STM32 接收数据并发布 ROS2 话题，`robot_gateway` 订阅后每秒写
入一条遥测记录：

```text
battery_state
env/temperature
env/humidity
battery/temperature
base/faults
imu/raw
odom
```

K230 通过 HTTP 上传识别事件，网关同时：

1. 保存原始识别 JSON 到 `vision_events`。
2. 把每个 `rust` 目标拆开保存到 `vision_detections`。
3. 更新 `/api/v1/latest`，供 Web 页面显示。

### 8.2 SQLite 数据库

默认路径：

```text
/root/robot_data/robot.db
```

主要表：

| 表 | 内容 |
| --- | --- |
| `telemetry` | 电池、温湿度、故障、里程计等周期快照 |
| `vision_events` | K230 每次上传的识别帧 |
| `vision_detections` | 每个类别、置信度和框坐标 |
| `control_events` | Web、K230、停车看门狗的控制日志 |

不要把每一帧完整图片直接写入 eMMC 数据库。第一版保存框、类别和时间即可；需要图片
留档时，把图片按日期写入 SD 卡，再把路径写入数据库。

## 9. 严格的整机联调顺序

### 第 1 轮：无电机、无 K230

- STM32 只接 5V、传感器和调试串口。
- 确认 DHT30、MPU6050、DS18B20、INA219 数据。
- 确认没有持续的欠压或传感器故障。

### 第 2 轮：STM32 架空电机

- 轮子离地。
- 使用极小速度。
- 检查左右轮方向、编码器方向、停车和 STBY。
- 保持继电器测试关闭。

### 第 3 轮：正式串口协议

- `ROBOT_UART1_DEBUG_ONLY=0`。
- KICKPI 启动 `robot_base_bridge`。
- 用 ROS2 话题确认零速度、使能和故障命令。

### 第 4 轮：启动 Web/API

- 启动 `robot_gateway`。
- 浏览器查看传感器状态。
- 浏览器只点“停止”和“使能”，不要立刻长按前进。
- 测试网关控制看门狗。

### 第 5 轮：K230 不开模型

- `ENABLE_VISION=False`。
- 测试屏幕、触摸、网络和方向按钮。
- K230 拔网线后，网关应在约 300 ms 后停车。

### 第 6 轮：K230 开模型

- `ENABLE_VISION=True`。
- 观察屏幕是否出现 `rust` 框。
- 检查 KICKPI 数据库是否有 `vision_events` 和 `vision_detections`。
- 观察识别运行几分钟后是否内存稳定、画面不冻结。

### 第 7 轮：整车低速

- 选择空旷区域。
- 先低速直行，再低速转向。
- 测试急停、清故障、通信断开、低电压和过温行为。
- 最后才进行连续运行和路径控制。

## 10. 当前最优先的未完成事项

按优先级排列：

1. 完成 INA219 的真实电池正负极、VCC、GND、SDA、SCL 接线，确认电压值正确。
2. 确认底板 5V、12V、GND 的电源线焊接可靠，电机大电流回路不要经过 2.54 mm 小信号座子。
3. 保持 STM32 文本调试，完成传感器和编码器最终验收。
4. 关闭继电器 5 秒周期测试，关闭蜂鸣器。
5. 将 `ROBOT_UART1_DEBUG_ONLY` 改为 `0`，重新编译烧录正式固件。
6. 在 KICKPI 手动启动 `robot_base_bridge`，确认真实 STM32 数据进入 ROS2。
7. 手动启动 `robot_gateway`，浏览器检查真实数据和控制命令。
8. 把 `k230_canmv_apps` 内容复制到 K230 SD 卡，先不开模型测试屏幕和网络。
9. 打开 `AnchorBaseDet` 模型，检查 `rust` 框和视觉入库。
10. 全部通过后修改 Token，并启用 systemd 开机服务。

## 11. 最终开机服务

确认整机测试通过后，在 KICKPI 执行：

```bash
sudo systemctl daemon-reload
sudo systemctl enable robot-base-bridge.service
sudo systemctl enable robot-gateway.service
sudo systemctl start robot-base-bridge.service
sudo systemctl start robot-gateway.service
sudo systemctl status robot-base-bridge.service
sudo systemctl status robot-gateway.service
```

停用命令：

```bash
sudo systemctl stop robot-gateway.service
sudo systemctl stop robot-base-bridge.service
sudo systemctl disable robot-gateway.service
sudo systemctl disable robot-base-bridge.service
```

查看日志：

```bash
journalctl -u robot-base-bridge.service -f
journalctl -u robot-gateway.service -f
```

## 12. 最终验收标准

以下项目全部满足，才可以认为第一版完整闭环完成：

- STM32 上电不会自动驱动电机。
- KICKPI 未发送命令时，小车保持停止。
- 方向按钮松手后小车停止。
- K230 或 KICKPI 网络断开后小车自动停止。
- STM32 和 KICKPI 串口断开后小车自动停止。
- `enable`、`stop`、`clear_fault`、`estop` 行为正确。
- INA219 电压读数与万用表误差在可接受范围内。
- DHT30、DS18B20 和 MPU6050 连续运行数据稳定。
- 两个编码器方向和里程计方向正确。
- 屏幕实时显示摄像头画面和 `rust` 检测框。
- KICKPI Web 页面显示最新传感器、故障和视觉数据。
- SQLite 连续写入遥测和视觉事件，没有异常增长或频繁锁库。
- 继电器不再执行周期测试，只有收到明确控制逻辑时才动作。
- 蜂鸣器保持关闭，除非以后专门启用报警策略。
- 服务重启后能恢复，服务停止后小车仍处于安全停车状态。

## 13. 当前结论

现在项目已经从“单独底盘代码”推进到了“底盘 + KICKPI 网关 + K230 视觉控制程序”的
软件联调阶段。`Cron` 模型已经被正确识别为 `AnchorBaseDet/rust`，并且已经接入
`robot_touch_drive.py`，不是用错模型类型。

目前不能宣布整机已经完成，原因只有几个明确的实机环节：INA219 真实供电和读数、STM32
正式二进制协议、KICKPI 与底板实串口、K230 屏幕/摄像头/NPU 实机运行，以及最终电机和
安全策略验收。按照本文第 10 节的顺序完成，就能把现有工程推进到可运行版本。
