# K230 屏幕控制、视觉识别与 KICKPI Web 部署说明

更新时间：2026-08-27

本文结合 `D:\cxdownload\end\K230` 中的 CanMV-K230 资料和
`D:\cxdownload\end\Cron` 中的实际训练模型，给出当前代码的完整落地方法。
当前实现目标是：K230 屏幕显示摄像头画面，屏幕触摸按钮控制小车，K230 识别结果发送给
KICKPI，KICKPI 订阅底盘 ROS2 数据并写入 SQLite，浏览器通过 Web 页面查看和控制。

## 一、最终连接关系

```text
K230 摄像头 + LCD/触摸屏
        |
        | 以太网 HTTP JSON
        v
KICKPI H618 192.168.1.119
        |
        | robot_gateway:8080
        | ROS2 /cmd_vel、/base/state_cmd
        v
robot_base_bridge
        |
        | /dev/ttyAS5, 115200
        v
STM32 最小系统板 -> TB6612 模块 -> 两个编码电机
```

K230 不直接连接 STM32，也不直接输出电机 PWM。KICKPI 是唯一的上层控制网关，
STM32 仍然保留通信超时、故障、急停等实时保护。

## 二、资料中已经确认的接口

本次使用了以下资料中的接口和示例：

| 资料位置 | 用途 |
| --- | --- |
| `K230\code\examples\14-Socket\network_lan.py` | K230 有线 LAN 的 DHCP/静态 IP |
| `K230\code\examples\14-Socket\http_client.py` | K230 HTTP socket 客户端思路 |
| `K230\code\examples\03-Machine\display_and_touch.py` | `ST7701` LCD 与 `TOUCH(0)` |
| `K230\code\libs\PipeLine.py` | 摄像头绑定显示层和 OSD 叠加 |
| `Cron\det_video.py` | 你的 Canaan/K230 AnchorBaseDet 视频部署入口 |
| `Cron\det_image.py` | 你的模型单张图验证入口 |
| `Cron\mp_deployment_source\deploy_config.json` | 模型类别、anchors、输入尺寸和阈值 |
| `K230\code\libs\PlatTasks.py` | `DetectionApp` 的 AnchorBaseDet 后处理 |

资料中的 LCD 例程使用 `800x480` 和 `Display.ST7701`，本实现采用同样的分辨率。
你的 Cron 部署包不是普通 YOLOv8 模型，而是 `AnchorBaseDet`。配置已经确认如下：

| 参数 | 当前值 |
| --- | --- |
| 模型文件 | `best_AnchorBaseDet_can2_5_s_20260827131049.kmodel` |
| 模型类型 | `AnchorBaseDet` |
| 识别类别 | `rust` |
| 类别数量 | `1` |
| 模型输入 | `320x320` |
| 摄像头 AI 通道 | `640x360` |
| 置信度阈值 | `0.4` |
| NMS 阈值 | `0.5` |
| NMS 模式 | 类内 NMS，`nms_option=false` |
| nncase 版本 | `2.11.0` |

你提供的 `det_results` 图片已经看到 `rust` 检测框，说明模型输出和后处理结果是合理的；
但本次还没有把程序放到 K230 实机上执行，因此实机帧率、屏幕驱动和摄像头兼容性仍需现场验证。

模型结果在 K230 内部的格式是：

```text
{"boxes": [[x1, y1, x2, y2]], "scores": [score], "idx": [class_id]}
```

当前程序会把它转换成 KICKPI 接口需要的 `x/y/w/h` JSON 数组，类别 `0` 由配置文件解析为 `rust`。

## 三、已经生成的文件

### 1. K230 文件

```text
D:\cxdownload\end\code\k230_canmv_apps\robot_touch_drive.py
```

复制到 K230 SD 卡后运行。该程序包含：

- 摄像头实时画面；
- 800x480 触摸按钮：前进、后退、左转、右转、停车；
- 使能、清故障、急停；
- KICKPI 网络在线/离线和故障码提示；
- 你的 `AnchorBaseDet` 锈病 `rust` 检测框；
- 识别结果每 500 ms 发送一次；
- 方向按钮按住运行，松开停车；
- KICKPI 300 ms 收不到控制心跳时自动停车；
- HTTP 或 AI 出错时不阻塞退出，程序最终仍会发送停车命令。

另外，部署目录中还加入了单张图片验证文件：

```text
D:\cxdownload\end\code\k230_canmv_apps\det_image.py
D:\cxdownload\end\code\k230_canmv_apps\test.jpg
```

复制到 K230 后，可以先运行 `det_image.py` 验证 NPU、模型、配置和后处理，再运行
`robot_touch_drive.py` 做摄像头视频和触摸控制联调。

### 2. KICKPI ROS2 包

```text
D:\cxdownload\end\code\kickpi_ros2_ws\src\robot_gateway
```

包中包含：

- `gateway_node.py`：HTTP API、ROS2 订阅/发布、SQLite 写入；
- `web/index.html`：浏览器控制与数据页面；
- `launch/gateway.launch.py`：ROS2 启动文件。

### 3. 已更新的底盘桥接节点

```text
D:\cxdownload\end\code\kickpi_ros2_ws\src\robot_base_bridge\robot_base_bridge\base_bridge_node.py
```

新增 ROS2 话题：

```text
/base/state_cmd   std_msgs/msg/String
```

可发送：

```text
enable
stop
clear_fault
estop
```

## 四、KICKPI 部署步骤

### 4.1 从 Windows 复制代码到 KICKPI

可用 `scp`，在 Windows PowerShell 中执行：

```powershell
scp -r D:\cxdownload\end\code\kickpi_ros2_ws\src\robot_gateway root@192.168.1.119:/root/kickpi_ros2_ws/src/
scp -r D:\cxdownload\end\code\kickpi_ros2_ws\src\robot_base_bridge root@192.168.1.119:/root/kickpi_ros2_ws/src/
```

如果 KICKPI 上工作空间路径不是 `/root/kickpi_ros2_ws`，把目标路径换成实际路径。

### 4.2 编译 ROS2 工作空间

登录 KICKPI：

```bash
ssh root@192.168.1.119
cd /root/kickpi_ros2_ws
source /opt/ros/humble/setup.bash
rosdep install --from-paths src --ignore-src -r -y
colcon build --symlink-install
source install/setup.bash
```

若系统缺少 pyserial，安装：

```bash
apt-get update
apt-get install -y python3-serial
```

### 4.3 第一次手动启动

终端一启动底盘串口桥：

```bash
source /opt/ros/humble/setup.bash
source /root/kickpi_ros2_ws/install/setup.bash
ros2 launch robot_base_bridge base_bridge.launch.py port:=/dev/ttyAS5 baud:=115200
```

终端二启动 Web/API 网关：

```bash
source /opt/ros/humble/setup.bash
source /root/kickpi_ros2_ws/install/setup.bash
mkdir -p /root/robot_data
ros2 launch robot_gateway gateway.launch.py \
  host:=0.0.0.0 \
  port:=8080 \
  db_path:=/root/robot_data/robot.db \
  token:=请替换成你自己的长随机字符串 \
  control_timeout_s:=0.3
```

注意：K230 程序里的 `API_TOKEN` 也必须改成同一个字符串。

### 4.4 检查 KICKPI 状态

```bash
source /opt/ros/humble/setup.bash
source /root/kickpi_ros2_ws/install/setup.bash
ros2 topic list
ros2 topic echo /base/faults
ros2 topic echo /battery_state
ros2 topic echo /env/temperature
ros2 topic echo /env/humidity
ros2 topic echo /odom
curl http://127.0.0.1:8080/api/v1/health
curl http://127.0.0.1:8080/api/v1/latest
```

浏览器打开：

```text
http://192.168.1.119:8080
```

浏览器通过 `X-Device-Token` 请求头发送控制命令。页面默认从浏览器的
`localStorage.robot_token` 读取令牌；若改过令牌，可在浏览器控制台执行：

```javascript
localStorage.setItem("robot_token", "你的新令牌")
```

然后刷新页面。

## 五、KICKPI API

### 5.1 健康检查

```text
GET /api/v1/health
```

不需要令牌。

### 5.2 最新状态

```text
GET /api/v1/latest
```

返回电池电压、DHT30 温湿度、DS18B20 温度、MPU6050、里程计、故障码和最后一次视觉结果。

### 5.3 底盘速度

```text
POST /api/v1/control/cmd_vel
X-Device-Token: 你的令牌
Content-Type: application/json
```

请求示例：

```json
{
  "source": "test",
  "button": "forward",
  "linear_x_m_s": 0.05,
  "angular_z_rad_s": 0.0
}
```

单位是 m/s 和 rad/s。网关默认限幅为：

```text
线速度：±0.20 m/s
角速度：±1.20 rad/s
```

### 5.4 状态命令

```text
POST /api/v1/control/state
X-Device-Token: 你的令牌
Content-Type: application/json
```

请求示例：

```json
{"source":"test","command":"enable"}
```

`command` 可用：`enable`、`stop`、`clear_fault`、`estop`。

### 5.5 K230 视觉结果

```text
POST /api/v1/vision/events
X-Device-Token: 你的令牌
Content-Type: application/json
```

请求示例：

```json
{
  "source": "k230",
  "frame_id": 1523,
  "detections": [
    {
      "label": "rust",
      "class_id": 0,
      "score": 0.93,
      "x": 120,
      "y": 80,
      "w": 190,
      "h": 360
    }
  ]
}
```

数据库保存原始 JSON，同时拆开保存每个目标，方便以后查询。

## 六、K230 SD 卡准备

### 6.1 最小运行方式

把当前目录中的以下内容复制到 K230 SD 卡根目录 `/sdcard/`：

```text
/sdcard/robot_touch_drive.py
/sdcard/det_image.py
/sdcard/test.jpg
/sdcard/libs/            <- K230/code/libs 整个目录
/sdcard/mp_deployment_source/
    deploy_config.json
    best_AnchorBaseDet_can2_5_s_20260827131049.kmodel
```

当前已经整理好的本地可复制目录是：

```text
D:\cxdownload\end\code\k230_canmv_apps\
    robot_touch_drive.py
    libs\
    mp_deployment_source\
        deploy_config.json
        best_AnchorBaseDet_can2_5_s_20260827131049.kmodel
```

其中 `libs` 是从 `D:\cxdownload\end\K230\code\libs` 复制的，模型和配置是从
`D:\cxdownload\end\Cron\mp_deployment_source` 复制的。当前模型文件 SHA256 为：

```text
E6E49E7015F399F6AB3259E34E202439F61CB7C6A5EA80D3DDFD658318625228
```

复制到 K230 后可以在 K230 的 REPL 中检查文件是否存在：

```python
import os
print(os.stat("/sdcard/mp_deployment_source/deploy_config.json"))
print(os.stat("/sdcard/mp_deployment_source/best_AnchorBaseDet_can2_5_s_20260827131049.kmodel"))
```

复制完成后，K230 上的模型必须位于：

```text
/sdcard/mp_deployment_source/best_AnchorBaseDet_can2_5_s_20260827131049.kmodel
```

配置文件中的 `kmodel_path` 必须与模型文件名一致。程序启动时会自动读取：

```text
/sdcard/mp_deployment_source/deploy_config.json
```

### 6.2 修改 K230 参数

打开 `robot_touch_drive.py`，先确认：

```python
KICKPI_IP = "192.168.1.119"
KICKPI_HTTP_PORT = 8080
API_TOKEN = "和 KICKPI 一致的令牌"
```

有 DHCP 的路由器使用：

```python
USE_DHCP = True
```

K230 直接网线连接 KICKPI，或现场没有 DHCP 时使用静态地址：

```python
USE_DHCP = False
STATIC_IP = "192.168.1.120"
STATIC_MASK = "255.255.255.0"
STATIC_GATEWAY = "192.168.1.1"
STATIC_DNS = "192.168.1.1"
```

网线直连时两个设备都要配置同一网段，网关可以填 `0.0.0.0` 或不使用。

### 6.3 不带模型先测试

先改：

```python
ENABLE_VISION = False
```

这时程序只测试：

- 摄像头画面；
- LCD 显示；
- 触摸按钮；
- K230 到 KICKPI 的 HTTP 控制；
- KICKPI 到 STM32 的 `/cmd_vel`。

底盘和网络都确认后，再放入模型，改回：

```python
ENABLE_VISION = True
```

如果模型路径不存在，程序会自动关闭视觉功能，但保留画面和触摸控制。

## 七、推荐测试顺序

### 第一步：不接电机，测试 KICKPI 网关

```bash
curl -H 'X-Device-Token: 你的令牌' \
  -H 'Content-Type: application/json' \
  -d '{"source":"curl","button":"stop","linear_x_m_s":0,"angular_z_rad_s":0}' \
  http://127.0.0.1:8080/api/v1/control/cmd_vel
```

### 第二步：STM32 断开电机，确认话题

```bash
ros2 topic echo /base/faults
ros2 topic echo /odom
ros2 topic echo /battery_state
```

### 第三步：电机悬空，先 `enable` 再短按方向

先在 Web 页面点击“使能”，然后只短按“前进”。确认：

- 松手后电机停止；
- K230 网线拔掉后不超过 300 ms 停止；
- KICKPI 网关退出后 STM32 自身通信超时停车；
- 故障码出现时网关拒绝非零速度。

### 第四步：接入 K230，不带模型

确认屏幕按钮和 Web 页面都能控制小车。此阶段不要求识别。

### 第五步：接入 Cron 锈病模型

确认屏幕上出现 `rust` 检测框，KICKPI 上检查：

```bash
curl http://127.0.0.1:8080/api/v1/vision/events?limit=5
sqlite3 /root/robot_data/robot.db 'select * from vision_events order by id desc limit 5;'
```

## 八、数据库说明

默认数据库：

```text
/root/robot_data/robot.db
```

表：

| 表 | 内容 |
| --- | --- |
| `telemetry` | 每秒一次的电池、温湿度、故障、里程计快照 |
| `vision_events` | K230 每次上传的识别帧原始 JSON |
| `vision_detections` | 拆分后的类别、置信度和目标框 |
| `control_events` | Web、K230 和 watchdog 的控制日志 |

建议每天或每次实验结束后备份：

```bash
cp /root/robot_data/robot.db /root/robot_data/robot-$(date +%F-%H%M%S).db
```

不要把每一帧原始图片写入 SQLite；这会快速增加 eMMC 写入量。第一版只保存检测框和
类别，确实需要留图时再按日期把图片保存到 SD 卡。

## 九、当前实现的边界

1. Web 页面目前使用 1 秒轮询，不是 WebSocket；功能验证够用，后续再升级推送。
2. K230 程序当前读取 Cron 的 `deploy_config.json`，类别是 `rust`。如果重新训练模型，必须
   同步替换 `deploy_config.json` 和对应的 `.kmodel`，不能只替换模型文件。
3. K230 屏幕按钮控制的是 `/cmd_vel`，最终左右轮换算、编码器闭环和电机保护仍由 STM32 执行。
4. `enable` 只代表允许底盘进入运行状态，不会自动让车运动。
5. 急停接口会发送 `estop` 到 STM32；若你的 STM32 固件把急停定义成锁存状态，必须点击
   “清故障”后再点击“使能”。
6. 不要直接把 `8080` 暴露到公网；只在可信局域网使用，并立即修改默认 Token。
