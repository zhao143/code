# 蓝牙与多控制端控制说明

最后更新：2026-09-02

## 1. 本次完成内容

本次把“手机蓝牙、KICKPI 网页/ROS、K230 屏幕”三类控制端统一到同一套底盘控制规则中：

```text
手机 App / 蓝牙助手
        |
        | 经典蓝牙 SPP，文本命令
        v
STM32 USART2（PA2/PA3）

KICKPI ROS2 / Web / K230
        |
        | UART5 -> STM32 USART1，二进制协议
        v
STM32 USART1（PA9/PA10）
```

K230 不直接连接 STM32。K230 触摸控制仍然通过以太网请求 KICKPI，再由 KICKPI 转成底盘二进制运动命令。

已经修改的主要文件：

```text
D:\cxdownload\end\code\Core\Inc\robot_config.h
D:\cxdownload\end\code\Core\Inc\robot_app.h
D:\cxdownload\end\code\Core\Inc\robot_comm.h
D:\cxdownload\end\code\Core\Inc\robot_bluetooth.h
D:\cxdownload\end\code\Core\Src\robot_app.c
D:\cxdownload\end\code\Core\Src\robot_comm.c
D:\cxdownload\end\code\Core\Src\robot_bluetooth.c
D:\cxdownload\end\code\.eide\eide.yml
D:\cxdownload\end\code\MDK-ARM\code.uvprojx
D:\cxdownload\end\code\kickpi_ros2_ws\src\robot_base_bridge\robot_base_bridge\base_bridge_node.py
```

手机 App 项目：

```text
D:\cxdownload\end\code\Car_Control
```

## 2. STM32 当前固件开关

文件：

```text
D:\cxdownload\end\code\Core\Inc\robot_config.h
```

当前正式运行配置：

```c
#define ROBOT_UART1_DEBUG_ONLY            0
#define ROBOT_BLUETOOTH_ENABLE            1
#define ROBOT_BUZZER_ENABLE               0
#define ROBOT_RELAY_ENABLE                0
#define ROBOT_MOTOR_AUTO_TEST_ENABLE      0
#define ROBOT_BLUETOOTH_OWNER_TIMEOUT_MS  800U
```

含义：

- `USART1` 使用 KICKPI 正式二进制协议，不再输出可读调试文本。
- `USART2` 启用蓝牙文本协议。
- 蜂鸣器和继电器仍然关闭，符合当前“蓝牙加入但蜂鸣器、继电器暂不参与控制”的要求。
- 自动电机测试关闭，避免底板上电后自行转动。
- 蓝牙运动命令超过 800ms 没有刷新，STM32 自动停车并释放蓝牙控制权。

## 3. 蓝牙模块接线

当前 STM32 工程的 USART2 配置为 `9600 8N1`：

| 蓝牙模块 | STM32 底板 |
| --- | --- |
| TXD | PA3 / USART2_RX |
| RXD | PA2 / USART2_TX |
| GND | GND |
| VCC | 按模块规格供电，当前设计按 5V 供电 |

注意：

1. 必须交叉连接 TX 和 RX。
2. 蓝牙模块、STM32、KICKPI 和电源必须有公共 GND。
3. 只有标明支持 5V 供电的模块才接 5V。若模块 VCC 只允许 3.3V，不要直接接 5V。
4. PA2/PA3 是 3.3V UART。大多数 HC-05/HC-06 模块的串口逻辑可以直接与 STM32 连接，但更换模块时必须重新确认 RXD 的电平要求。
5. 蓝牙模块如果接在 5V 电源上，不能把它的 5V 电源脚误接到 STM32 的 3.3V 信号脚。

## 4. 蓝牙文本协议

每条命令必须以换行结束。手机 App 自动发送 `CRLF`，蓝牙助手需要打开“发送换行”或手动发送回车换行。

### 4.1 基础命令

```text
help
status
enable
stop
clear
clear_fault
estop
```

### 4.2 PWM 开环运动

范围为 `-1000` 到 `1000`：

```text
pwm 300 300
pwm -300 -300
pwm -300 300
pwm 300 -300
pwm 0 0
```

参数含义：

```text
pwm <A轮目标> <B轮目标>
```

方向符号以当前电机接线和 `ROBOT_MOTOR_A_INVERT`、`ROBOT_MOTOR_B_INVERT` 配置为准。第一次测试时让轮子悬空，低 PWM 开始。

### 4.3 速度闭环

单位是毫米每秒：

```text
speed 50 50
speed -50 -50
```

第一次联动测试建议使用 `pwm`，确认编码器方向、每圈计数和轮径后再使用 `speed`。

### 4.4 方向快捷命令

```text
forward
back
left
right
forward 300
back 300
left 250
right 250
```

不填写幅值时默认 PWM 为 300。

### 4.5 返回文本

正常运动命令会返回：

```text
OK motion mode=0 target=300,300 owner=BLUETOOTH
```

控制权被其他控制端占用时返回：

```text
BUSY owner=KICKPI
```

存在故障时返回：

```text
FAULT code=0x0002
```

状态命令返回示例：

```text
STATUS state=2 fault=0x0000 owner=BLUETOOTH target=300,300 pwm=275,280 bat=12000mV
```

## 5. 三类控制端的仲裁规则

控制权只有一个，状态字段会显示：

```text
NONE
KICKPI
BLUETOOTH
UART1_DEBUG
```

规则如下：

1. 没有控制者时，第一个有效的非零运动命令获得控制权。
2. 当前控制者可以持续刷新自己的运动命令。
3. 另一个来源发送非零运动命令时，不改变当前目标，返回忙状态。
4. `stop` 是全局安全命令，任何来源都可以停车并释放控制权。
5. 蓝牙 App 按住方向键时每 100ms 发送一次 PWM；松手立即发送 `stop`。
6. 蓝牙超过 800ms 没有刷新，STM32 自动停车并释放蓝牙控制权。
7. KICKPI 的 ROS2 控制仍然由固定周期的二进制命令刷新；KICKPI 失联超过约 700ms 时进入通信超时保护并停车。
8. KICKPI 发送的周期性零速度刷新不会打断正在使用蓝牙的控制端。
9. `estop` 无论来自哪里都会急停并锁存故障；需要 `clear` 后才能恢复。
10. `enable` 不占用运动控制权，只把无故障底盘置为 READY。

因此，实际使用建议是：

```text
手机蓝牙控制时：
1. 手机 App 连接蓝牙
2. 点击“使能”
3. 按住方向键
4. 松手停车

切回 KICKPI 时：
1. 手机松开方向键或点击“停车”
2. 等待蓝牙控制权释放
3. KICKPI 发送 0 速度
4. 再发送 ROS2 运动命令
```

## 6. 手机 App 的使用方法

项目目录：

```text
D:\cxdownload\end\code\Car_Control
```

App 当前功能：

- 申请 Android 12 及以上的“附近设备”权限。
- 读取系统已经配对的经典蓝牙设备。
- 连接 HC-05/HC-06/兼容 SPP 的 UART 蓝牙模块。
- 显示发送命令和 STM32 返回文本。
- 速度滑块范围为 100 到 700。
- 方向键按住运动、松开停车。
- 提供“使能、停车、清故障、读取状态、断开并停车”按钮。

首次使用：

1. 给底板和蓝牙模块上电。
2. 在手机系统蓝牙设置中搜索并配对模块。
3. 输入模块要求的 PIN。常见值是 `1234` 或 `0000`，以模块说明书为准。
4. 打开 App，允许“附近设备”权限。
5. 在“已配对设备”中点击“连接”。
6. 看到“已连接”后点击“使能”。
7. 车轮悬空，先按住“前”低速测试。
8. 松开手指，确认电机立即停止。
9. 查看通信日志，确认有 `手机：pwm ...` 和 `底盘：OK ...`。

本 App 使用经典蓝牙 SPP，不适用于只有 BLE GATT 服务的模块。如果以后换成 BLE 模块，需要另写 BLE 扫描、服务发现和特征值收发代码。

## 7. 编译和安装 App

在 Android Studio 中打开：

```text
D:\cxdownload\end\code\Car_Control
```

命令行构建：

```powershell
cd D:\cxdownload\end\code\Car_Control
.\gradlew.bat :app:assembleDebug
```

生成的 APK 通常位于：

```text
D:\cxdownload\end\code\Car_Control\app\build\outputs\apk\debug\app-debug.apk
```

也可以直接在 Android Studio 中点击运行按钮安装到手机。

## 8. 烧录 STM32 后的验证顺序

1. 确认 USART2 的 `PA2/PA3` 没有被其他功能复用。
2. 确认蓝牙模块 TX 接 PA3，RX 接 PA2，GND 共地。
3. 确认 TB6612 模块更换为已经验证正常的那一块。
4. 车轮悬空上电。
5. 先通过蓝牙助手发送：

```text
status
enable
pwm 100 0
stop
pwm 0 100
stop
```

6. 确认 A、B 两路都能单独转动和停止。
7. 再打开手机 App 测试方向键。
8. 蓝牙验证完成后，再连接 KICKPI 的 UART1 正式二进制链路。
9. KICKPI 第一条命令始终发送 0 速度。
10. 最后才测试 Web、ROS2 和 K230 屏幕控制。

## 9. 当前验证状态

已完成：

- STM32 蓝牙接收环形缓冲、换行组包和发送函数。
- USART2 中断收发接入。
- 手机 App 经典蓝牙 SPP 管理器。
- 手机 App 控制界面和按住刷新、松开停车逻辑。
- KICKPI 状态解析兼容新增的 `control_owner` 字段。
- EIDE 和 Keil 工程加入 `robot_bluetooth.c`。
- KICKPI Python 节点通过 `py_compile` 语法检查。

尚未完成：

- 需要用 Keil 或 EIDE 实际生成新的 STM32 HEX 并烧录。
- 需要使用蓝牙模块做真实收发测试。
- 需要在真实 Android 手机安装 App，验证权限、配对、连接和按钮事件。
- 需要验证蓝牙控制和 KICKPI 控制切换时的控制权状态。
- 本机 Gradle 当前在启动守护进程时出现 loopback connection 错误，源码已写入，但 APK 还需要在 Android Studio 或修复该本机 Gradle 环境后构建。

## 10. 安全提醒

- 所有第一次电机测试都让车轮悬空。
- App 的“停车”按钮和 STM32 的 800ms 蓝牙超时只是软件保护，不能替代电源总开关。
- 发生 `FAULT` 或 `ESTOP` 后，不要直接提高 PWM；先读取 `status`，确认故障原因。
- 传感器未接好时，可能出现低压或传感器无效状态，先确认供电、GND 和 I2C 接线。
- 当前蜂鸣器、继电器仍关闭；不要把 App 中的停车测试误认为继电器或蜂鸣器已经启用。
